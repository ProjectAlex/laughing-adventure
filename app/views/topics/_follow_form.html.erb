  <div id="follow_form">
    <% if current_user.following?(@topic) %>
      <%= render 'topics/unfollow', topic: @topic %>
    <% else %>
      <%= render 'topics/follow', topic: @topic %>
    <% end %>
  </div>
