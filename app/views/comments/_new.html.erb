<script>

</script>
<%= form_for([post, post.comments.build]) do |f| %>
    <%= f.text_area :body , {:onfocus => "disable()" , :onblur => "enable()" , :required => true} %>

    <%= f.button :Comment , {:class => 'btn-primary' , :id => 'comment-btn'} %>
<% end %>
