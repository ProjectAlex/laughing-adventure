<%= provide(:title,@user.name) %>
<h3><%= @user.name %></h3>
<div class="row">
<div class="span3">
<%= link_to "#{image_tag (@user.avatar.url(:large)) , size:'200x200',class: 'img-thumbnail'}".html_safe , @user.avatar.url%>
<%# image_tag @user.avatar.url(:large), :size => "200", :class => 'img-thumbnail' %><br><br>
<p>Email : <%= @user.email if @user.email %></p>
<p>DOB : <%= @user.date_of_birth %></p>
</div>
<div class="span8">
<div id="postsfeed">
    <%= render :partial => 'home/posts.html.erb', :locals => { :posts_streams => @user_posts } %>
</div>
</div>
